// import "./pub-info.scss";

const PubInfo = ({ selectedPub }) => {
  //   const { pub, address } = selectedPub;

  //   console.log(selectedPub.pub);

  return (
    <div className="container">
      {/* <article className="friend-card">
        <div className="friend-card__profile"></div>
        <h2 className="friend-title">{pub}</h2>
        <p className="friend-card__text">{address.latitude}</p>
        <p className="friend-card__text">{address.longitude}</p>
      </article> */}
    </div>
  );
};

export default PubInfo;
